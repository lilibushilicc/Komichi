# ── Android / Gradle ──
android/app/build/
android/build/
android/.gradle/
android/.kotlin/
android/local.properties
android/keystore/
*.apk
*.aab
*.dex
*.class

# ── Node.js / Worker ──
worker/node_modules/
worker/.wrangler/
worker/.dev.vars

# ── Python（全局，覆盖 cli/ 与 crawler-daemon/）──
**/__pycache__/
*.egg-info/
*.pyc
*.pyo
.venv/
venv/
cli/staging/

# ── crawler-daemon ──
crawler-daemon/logs/
crawler-daemon/config.json
crawler-daemon/.env

# ── IDE ──
.idea/
android/.idea/

# ── AI 工具本地状态（不上传）──
.workbuddy/
.playwright-mcp/

# ── Debug / Logs ──
*.log

# ── .NET (windows/) 构建产物 ──
windows/**/bin/
windows/**/obj/

# ── UI Reports ──
komichi-ui/*-report.json
komichi-ui/*-summary.json

# ── OS ──
.DS_Store
Thumbs.db
