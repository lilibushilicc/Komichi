# Komichi 项目不足审计

> 扫描时间：2026-08-02
> 范围：worker / crawler-daemon / cli / android / windows / komichi-ui + 根文档
> 方法：静态扫描（结构、配置、文档与代码一致性、安全与工程化）

## 总体评价

架构分层清晰、文档整体详尽、"爬虫单一事实来源"的设计是对的。但**工程化成熟度偏低**：零测试、无 CI、文档与实现明显分叉、缺少发布治理与安全基线。以下按严重度列出。

---

## A. 工程质量（高）

### A1. 零自动化测试
全仓库**没有任何真实测试用例**。扫描命中 `*test*`/`*spec*` 仅来自 `worker/node_modules`（依赖自带）与 `android/app/build`（Gradle/KSP 缓存产物），源码中无 `*.test.ts` / `*.py` 测试 / Android `androidTest`。
- 爬虫是大量正则 HTML 解析（如 `worker/src/crawler/sfacg.ts` 用 9+ 条正则提取标题/封面/章节），源站改版即崩，却无回归测试。
- 影响：双端（TS + Python）爬虫行为无法保证一致，重构无防护网。

### A2. 无 CI / CD
根目录无 `.github/workflows`、GitLab CI、CircleCI 等任何流水线配置。提交不会触发 typecheck / build / test。

### A3. `worker` 提交门槛无法执行
`DEVELOPMENT.md` 规定提交前必须 `npm run typecheck` 与 `npm run build`，但 `worker/package.json` 的 scripts 只有 `dev/deploy/db:*`，**两个脚本都不存在**：
```json
"scripts": { "dev":"wrangler dev", "deploy":"wrangler deploy", "db:init":..., "db:seed":..., "db:create":..., "cf-typegen":"wrangler types" }
```
文档规定的质量关卡实际上跑不起来。

---

## B. 文档与实现分叉（高）

### B1. Worker 源数量被严重低估
`worker/src/crawler/registry.ts` 实际注册 **6 个** HTTP 源：
```
mh160mh, tencent, guazi, kuaikan, dongmanmanhua, sfacg
```
但 `README.md` / `DEPLOYMENT.md` / `DEVELOPMENT.md` 表格均称 Worker 仅支持 **4 个 HTTP 源**（mh160mh/tencent/guazi/kuaikan）。其中 `sfacg.ts` 是完整可用的爬虫，`dongmanmanhua` 也已注册——文档既漏写又可能掩盖未完成的源。

### B2. 已实现端点文档未收录
`worker/src/routes/work.ts` 已实现但 `USAGE.md` 的「API 接口文档」未列出的端点：
- `GET  /api/work/search`（VPS 搜索代理）
- `POST /api/work/import`（服务器端导入，文档只在 DEPLOYMENT 提到）
- `POST /api/work/import-via-vps`
- `GET/PUT /api/work/vps-url`
- `GET  /api/work/refresh-all`
文档滞后于实现，新用户无从得知这些能力。

---

## C. 安全（中-高）

### C1. 生产地址硬编码进仓库
`worker/wrangler.toml`：
```toml
[vars]
VPS_URL = "https://crawler.270312.xyz"
```
真实 VPS 域名暴露在版本库中（且非占位符），属信息泄漏，应改为部署期注入。

### C2. 默认弱口令无强制改密
`init.sql` 写入 `admin/admin123` 与 `crawler/crawler123`。仅在文档"提醒修改"，无首次登录强制改密、无密码强度校验、重复跑 `init.sql` 可能异常。

### C3. 免鉴权的公开搜索代理
`work.ts` 注释明确 `GET /api/work/search` **"无需鉴权"**，作为代理转发到 VPS `/api/search`。未授权即可触发对 VPS 的外呼，存在滥用 / DoS 风险。

### C4. 生产残留调试端点
`worker/src/routes/work.ts:337` 存在 `GET /api/work/debug-fetch/:id`（直接抓取并回显源站响应），属于调试接口，不应随生产部署暴露。

### C5. CORS 全开
`worker/src/index.ts:21` `app.use('*', corsMiddleware)` 注释"私有化部署允许所有来源"。若以多用户/公网形态部署，需收紧来源。

---

## D. 架构与可维护性（中）

### D1. 无 Monorepo 工作区
仓库是 6 个独立包（worker/crawler-daemon/cli/android/windows/komichi-ui），**无根 `package.json` / `pnpm-workspace.yaml`**，各端分别 `npm install` / `pip install`。跨端 API 契约全部靠人工同步，`DEVELOPMENT.md` 自己也承认"双端数据格式分叉"风险。

### D2. 无数据库迁移机制
`DEVELOPMENT.md` 提到"`worker/schema.sql` + 增量 `migrations/`"，但**仓库内不存在 `migrations/` 目录**。线上改表只能手动 `ALTER` 或重建，无版本化、无回滚、`schema.sql` 用 `CREATE TABLE IF NOT EXISTS` 不会迁移已有表。

### D3. Cron 追更缺乏伸缩与防护
`worker/src/index.ts` `refreshAllWorks()` 一次性 `SELECT` 全部作品并**串行**逐个爬取，无分页、无并发上限、无去重锁、无超时/重试预算。作品量大时会触碰 Worker CPU/时间上限，且单实例重复触发可能重复写入（仅按 `chapter_num` 去重，章节元数据变更不会更新）。

### D4. Web 阅读器脆弱的图片探测
`USAGE.md` 说明：阅读器按约定 `komichi/chapters/<work_id>/<chapter_num>/0000.jpg` 起**逐张探测**，无图片清单/总数。缺图即逐张 404 探测，体验差且浪费请求（CLI 章节上传本身也未实现，见 D5）。

### D5. CLI 章节图片上传未实现
`USAGE.md` 明确："当前 CLI 仅上传封面；章节图片上传尚未实现。" `sync` 流程不完整，章节图需手动按 R2 约定上传。

---

## E. 设计系统一致性（中，已记录）

### E1. 三端无统一 Design Token
- 品牌色：Web(`#32F08C`) & WPF(绿) vs **Android 蓝 `#8AB4F8`** 分裂。
- 语义色（Error / Border / 背景）三端数值互不相同。
- 三端均 dark-only，**无浅色 / 护眼模式**。

### E2. Web 图标按色预烘焙，不可主题化
`komichi-ui/assets/icons/trae/` 下大量同名多色 SVG：
`check.33c192.svg`、`plus.0c0c0d.svg`、`zap.0c0c0d.svg`、`atom.0c0c0d.svg`、`sparkles.32f08c.svg`、`alert.d27e24.svg`…
同一图标多个硬编码配色副本，**无法用 `currentColor` 改色 / 跟随主题**；且图标语义偏开发工具（terminal / code / git / cpu / atom / bug），与漫画阅读场景不匹配，缺漫画域图标（书本、书签、翻页等）。

---

## F. 版本与发布治理（低-中）

### F1. 各端版本号不一致
- `worker/src/index.ts` 根路由 `version: '2.0.0'`
- `worker/package.json` `version: '1.0.0'`
- `android/app/build.gradle.kts` `versionName = "1.1.0"`
- crawler-daemon `pyproject.toml` 未核对
缺统一版本源与 `CHANGELOG`。

### F2. 缺开源合规文件
根目录**无 `LICENSE`**（仓库却指向公开 GitHub），无 `CONTRIBUTING` / `SECURITY` / `CHANGELOG`。

### F3. Android Release 未开启混淆
`android/app/build.gradle.kts:44` `isMinifyEnabled = false`，release 包未压缩/混淆，且签名依赖本地 `keystore/`，无默认安全配置。

---

## G. Windows 端成熟度（中）

`windows/` 仅 ~17 个 `.cs` 文件，ViewModel 仅有 `Login / Main / Detail / Reader`，**缺 Shelf / History / Search 等页面**（Web 与 Android 均具备），功能覆盖明显少于另两端；视觉风格也与另两端不一致（见 E1）。

---

## H. 其他（低）

- `schema.sql` 使用 `TIMESTAMP`（D1 实际存字符串），且未对 `works(source_url)`、`chapters(work_id)` 建索引——这两列正是 cron 扫描与列表查询的热点。
- 无日志聚合 / 监控 / 告警：仅 `console.error`，生产错误不可观测。
- 用户/公开端点**无速率限制 / 滥用防护**。

---

## 建议优先级

| 优先级 | 行动 |
|--------|------|
| P0 | 移除 `wrangler.toml` 硬编码 VPS 域名；下线 `debug-fetch` 调试端点；`/search` 加鉴权或限流 |
| P0 | `worker` 补 `typecheck`/`build` 脚本，对齐文档；引入最少 CI（typecheck + Python compileall） |
| P1 | 补齐爬虫单测 + 关键路由单测；建立 `migrations/` 机制 |
| P1 | 校正文档（源数量、端点列表）；统一版本号与 LICENSE/CHANGELOG |
| P2 | 建立跨端 Design Token 单一来源；Web 图标改 `currentColor` 单色集 |
| P2 | cron 改为分页 + 并发预算 + 幂等锁；DB 加索引；CLI 实现章节上传 |
| P3 | Windows 端补齐 Shelf/History/Search；Android 开启 release 混淆 |
